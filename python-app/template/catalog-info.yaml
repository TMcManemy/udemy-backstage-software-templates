apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{values.app_name}}
  description: Python app for Udemy training
  annotations:
    github.com/project-slug: TMcManemy/${{values.app_name}}
spec:
  type: service
  owner: development
  lifecycle: experimental
